# ========================================
# Org Sync Service Environment Variables
# ========================================

# Required: Git repository URL (HTTPS or SSH)
# Example: https://github.com/username/org-files.git
REPO_URL=

# Required: Branch to sync
# Default is 'main' or 'master'
REPO_BRANCH=master

# Required: Org files to parse
# Should be path within the container. If multiple files, they should be comma separated
ORG_FILES="/data/repo/test.org,/data/repo/test2.org"

# Optional: GitHub Personal Access Token for HTTPS private repos
# Only needed if REPO_URL is HTTPS and private
GITHUB_TOKEN=

# Optional: Sync interval in minutes for scheduled sync
#           Sync interval controls the frequency of repo AND database updates
SYNC_INTERVAL_MINUTES=15

